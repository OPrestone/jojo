on:
push:
branches:
- main
name:
jobs:
Deploy website on push
web-deploy:
name: Deploy
runs-on: ubuntu-latest
steps:
- name: Get latest code
uses: actions/checkout@v3
- name: Install Node.js
uses:
with:
actions/setup-node@v3
node-version: 18.x
name: Install NPM packages
run: npm install
name: Build project
run: npm run build
Sync files I
name:
uses: Samkirkland/FTP-Deploy-Action@v4.3.4
        with:
          ftp-server: ftp.mix.co.ke
          ftp-username: gitaccess@jojosfashion.com
          ftp-password: ${{ secrets.FTP_PASSWORD }}  # Make sure to set up FTP_PASSWORD as a secret in your GitHub repository
          local-dir: ./fashion/  # Specify the local directory you want to deploy
